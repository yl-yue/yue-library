<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ai.yue.library.data.mybatis.mapper.SqlMapper">

    <select id="queryForJson" resultType="com.alibaba.fastjson2.JSONObject">
        ${sql}
    </select>

    <select id="queryForJsonList" resultType="com.alibaba.fastjson2.JSONObject">
        ${sql}
    </select>

    <select id="queryForStr" resultType="string">
        ${sql}
    </select>

</mapper>