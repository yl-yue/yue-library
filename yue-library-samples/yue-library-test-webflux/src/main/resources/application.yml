server:
  port: 8081
spring:
  application:
    name: yue-library-test-webflux
  profiles:
    include:    - yue-library-data-jdbc
    - yue-library-data-redis
    active:
    - dev
#yue:#  api-version:#    enabled: false
#  exception-handler:#    enabled: false